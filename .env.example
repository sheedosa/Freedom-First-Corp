# This site is a static build with no server-side runtime and no API keys.
# Nothing here is required to run, build or deploy it — `npm install && npm run dev`
# is enough. The file is kept only as the place to document future variables.
#
# Analytics is configured in index.html (Google Analytics tag), not via env vars.
